// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Replace with your real site URL before first deploy.
  // Drives sitemap, canonical links, OG/Twitter Cards.
  site: 'https://example.com',
  output: 'static',
});
