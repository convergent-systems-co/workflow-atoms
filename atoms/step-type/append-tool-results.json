{
  "schema": "https://workflow-atoms.com/schemas/atom-v1.json",
  "type": "step-type",
  "id": "workflow-atoms/step-type/append-tool-results",
  "version": "1.0.0",
  "name": "Append Tool Results",
  "description": "Append tool execution results to the running message context so the next LLM invocation receives the full conversation history including all tool outputs so far.",
  "inputs": [
    {"name": "context", "type": "array", "required": true, "description": "Current message context array"},
    {"name": "tool_calls", "type": "array", "required": true, "description": "Tool-call blocks from the previous assistant turn"},
    {"name": "tool_results", "type": "array", "required": true, "description": "Results from execute-tool-calls, in matching order"}
  ],
  "outputs": [
    {"name": "updated_context", "type": "array", "description": "Message context with the assistant tool-call turn and tool-result turn appended"},
    {"name": "context_length", "type": "integer", "description": "Number of messages in the updated context"}
  ],
  "retryable": false,
  "tags": ["context", "memory", "olympus", "agentic"]
}
