# Per-repo FUNDING.yml overrides the org default at convergent-systems-co/.github.
# Uncomment and edit only if this repo accepts sponsors directly.
#
# github: [your-username]
# custom: ["https://example.com/sponsor"]
